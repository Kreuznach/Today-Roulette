# 오늘의 룰렛 환경변수 예시
# .env.local 파일을 생성하고 아래 값을 실제 값으로 채우세요

# 광고 환경 설정
# test       → 공식 테스트 광고 ID 강제 사용
# production → 프로덕션 광고 ID 강제 사용
# 미설정     → 개발 빌드(DEV)이면 Mock, 프로덕션 빌드이면 실제 광고
# VITE_AD_ENV=test
